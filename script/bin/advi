#! /bin/bash

[[ -e /tmp/advi ]] || mkdir /tmp/advi
file=$(mktemp -p /tmp/advi)

EDITOR="urxvt -e sh -c nvim"
$EDITOR\ $file

[[ $? -eq 0 ]] && cat $file | xclip -selection PRIMARY
[[ $? -eq 0 ]] && cat $file | xclip -selection CLIPBOARD
